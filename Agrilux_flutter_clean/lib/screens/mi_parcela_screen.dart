import 'package:flutter/material.dart';

class MiParcelaScreen extends StatelessWidget {
  const MiParcelaScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Mi Parcela')));
}
